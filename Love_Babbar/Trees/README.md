# Trees
## Core Concept
Hierarchical data structures consisting of nodes and edges, usually Binary (at most two children). Heavily relies on Recursion for DFS Traversals (Inorder, Preorder, Postorder).
## Main Operations
- Depth First Search (DFS): `O(N)`
- Breadth First Search (BFS): `O(N)`
