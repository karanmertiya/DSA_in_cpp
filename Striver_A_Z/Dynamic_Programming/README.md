# Dynamic Programming
## Core Concept
An optimization over plain recursion. If a recursive function is solving the same subproblems repeatedly, we cache the results (Memoization) or build them iteratively (Tabulation).
## Main Operations
- Time Complexity: Exponential `O(2^N)` drops to Polynomial/Linear `O(N)`.
