# Heaps / Priority Queues
## Core Concept
Complete binary trees used to maintain a dynamically sorting collection of elements where the max (or min) is always at the root.
## Main Operations
- Push / Pop: `O(log N)`
- Peek: `O(1)`
